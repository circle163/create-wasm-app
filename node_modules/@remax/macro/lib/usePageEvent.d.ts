import { NodePath } from '@babel/traverse';
declare type Events = Set<string>;
export declare const pageEvents: Map<string, Events>;
export default function usePageEvent(path: NodePath, state: any): void;
export {};
