import { NodePath } from '@babel/traverse';
declare type Events = Set<string>;
export declare const appEvents: Map<string, Events>;
export default function useAppEvent(path: NodePath, state: any): void;
export {};
