import React from 'react';
export interface FilterProps {
    children?: React.ReactNode;
    style?: React.CSSProperties;
    zIndex?: number;
}
declare const InternalFilter: (props: FilterProps) => JSX.Element;
export default InternalFilter;
