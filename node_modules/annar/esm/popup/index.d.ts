import * as React from 'react';
export interface PopupProps {
    open?: boolean;
    children?: React.ReactNode;
    onClose?: () => void;
    closeable?: boolean;
    title?: React.ReactNode;
    position?: string;
    style?: React.CSSProperties;
    mask?: boolean;
    square?: boolean;
    curve?: string;
    transparent?: boolean;
}
declare const Popup: (props: PopupProps) => React.ReactElement;
export default Popup;
