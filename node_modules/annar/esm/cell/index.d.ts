import * as React from 'react';
import { InputProps } from '../input';
export interface CellProps {
    label?: React.ReactNode;
    children?: React.ReactNode;
    description?: React.ReactNode;
    style?: React.CSSProperties;
    labelStyle?: React.CSSProperties;
    valueStyle?: React.CSSProperties;
    verticality?: boolean;
    icon?: string;
    required?: boolean;
    border?: boolean;
    arrow?: boolean;
    extra?: React.ReactNode;
    field?: boolean;
    defaultNullValue?: string;
    onTap?: () => void;
}
declare const Cell: {
    (props: CellProps): JSX.Element;
    Picker(props: CellPickerProps): JSX.Element;
    Input(props: CellInputProps): JSX.Element;
};
export interface CellPickerProps extends CellProps {
    range?: any[];
    rangeKey?: string;
    disabled?: boolean;
    [propName: string]: any;
}
export interface CellInputProps extends CellProps, InputProps {
    [propName: string]: any;
}
export default Cell;
