import * as React from 'react';
export interface ValueProps {
    placeholder?: string;
    style?: React.CSSProperties;
    children?: React.ReactNode;
}
declare const Value: (props: ValueProps) => JSX.Element;
export default Value;
