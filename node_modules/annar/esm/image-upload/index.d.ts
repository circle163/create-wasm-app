import * as React from 'react';
export interface ImageProps {
    key: string;
    url: string;
}
export declare type DataItem = ImageProps | string;
export interface ImageUploadProps {
    files?: DataItem[];
    multiple?: boolean;
    multipleCount?: number;
    sizeType?: string[];
    sourceType?: string[];
    deletable?: boolean;
    disabled?: boolean;
    maxCount?: number;
    children?: React.ReactNode;
    onChange?: (e: DataItem[]) => void;
}
declare const ImageUpload: (props: ImageUploadProps) => JSX.Element;
export default ImageUpload;
