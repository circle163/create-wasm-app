import * as React from 'react';
export interface MaskProps {
    show?: boolean;
    onTap?: () => void;
    zIndex?: number;
    style?: React.CSSProperties;
    children?: React.ReactNode;
}
declare const Mask: (props: MaskProps) => React.ReactElement;
export default Mask;
