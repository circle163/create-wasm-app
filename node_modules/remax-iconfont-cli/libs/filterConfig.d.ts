import { Config } from './getConfig';
export declare const filterMiniProgramConfig: (config: Config, platform: string) => {
    symbol_url: string;
    save_dir: string;
    use_rpx: boolean;
    trim_icon_prefix: string;
    default_icon_size: number;
};
